# Here you can add your aliases and functions

#____________Tools____________#

# Git
alias g='git'
alias co='git checkout'
alias lgit='lazygit'

# Docker
alias d='docker'
alias dc='docker compose'

# Python
alias pip='python3 -m pip'

# Vim
alias vim='lvim'

# eza
if command -v eza >/dev/null; then
  alias ls='eza --group-directories-first --icons'
  alias ll='ls -l'
  alias la='ls -la'
  alias lt='ls --tree'
  alias lsg='ls --git'
fi


#___________Projects___________#

# Jamsync
alias jmsx='pnpm jmsx'

# Petit crèmier marketplace
alias crx='pnpm crx'
